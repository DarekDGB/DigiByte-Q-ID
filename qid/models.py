"""
Core data models for DigiByte Q-ID.

This file contains only placeholder structures.
We will slowly replace them as we build the real logic.
"""

from dataclasses import dataclass

@dataclass
class QIDPlaceholder:
    """Temporary placeholder model to keep structure valid."""
    note: str = "Replace with real Q-ID models later."
